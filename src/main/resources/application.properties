spring.application.name=RestDemo

# DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/greeshma?createDatabaseIfNotExist=true
spring.datasource.username=psamitha
spring.datasource.password=Sam@123

# JDBC Driver (mySQL)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Optional: Enable SQL logging (for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
